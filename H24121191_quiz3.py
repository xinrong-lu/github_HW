welcome = input("Welcome to the simple caculator program!")
firstnumber = float(input("Enter the first number: "))
secondnumber = float(input("Enter the second number: "))
arithmeoperation = input("Selevt an arithmetic operation (+, -, *, /): ")
#使輸出歡迎字樣,輸入first number,second number, arithmetic operation

if arithmeoperation == +:
	result = firstnumber + secondnumber
if arithmeoperation == -:
	result = firstnumber - secondnumber
if arithmeoperation == *:
	result = firstnumber * secondnumber
else:
	result = firstnumber / secondnumber	
#計算結果

print("Result:"+" "+result) #1
#輸出計算結果

anothor = input("Do you want to perfrom another calculation? (yes or no): ")
#輸出是否將進行下一個計算

if another == yes: 
	firstnumber = float(input("Enter the first number: "))
	secondnumber = float(input("Enter the second number: "))
	arithmeoperation = input("Selevt an arithmetic operation (+, -, *, /): ")

	if arithmeoperation == +:
	result = firstnumber + secondnumber
	if arithmeoperation == -:
	result = firstnumber - secondnumber
	if arithmeoperation == *:
	result = firstnumber * secondnumber
	else:
	result = firstnumber / secondnumber	
	#計算結果

	print("Result:"+" "+result)
	#若yes則繼續上則的計算循環
else:
	print("Goodbye!") #2
	#若yes之外的則輸出Goodbye字樣以作結,以下共進行五次計算

anothor = input("Do you want to perfrom another calculation? (yes or no): ")
#輸出是否將進行下一個計算

if another == yes: 
	firstnumber = float(input("Enter the first number: "))
	secondnumber = float(input("Enter the second number: "))
	arithmeoperation = input("Selevt an arithmetic operation (+, -, *, /): ")

	if arithmeoperation == +:
	result = firstnumber + secondnumber
	if arithmeoperation == -:
	result = firstnumber - secondnumber
	if arithmeoperation == *:
	result = firstnumber * secondnumber
	else:
	result = firstnumber / secondnumber	
	#計算結果

	print("Result:"+" "+result)
	#若yes則繼續上則的計算循環
else:
	print("Goodbye!")

anothor = input("Do you want to perfrom another calculation? (yes or no): ")
#輸出是否將進行下一個計算

if another == yes: 
	firstnumber = float(input("Enter the first number: "))
	secondnumber = float(input("Enter the second number: "))
	arithmeoperation = input("Selevt an arithmetic operation (+, -, *, /): ")

	if arithmeoperation == +:
	result = firstnumber + secondnumber
	if arithmeoperation == -:
	result = firstnumber - secondnumber
	if arithmeoperation == *:
	result = firstnumber * secondnumber
	else:
	result = firstnumber / secondnumber	
	#計算結果

	print("Result:"+" "+result)
	#若yes則繼續上則的計算循環
else:
	print("Goodbye!")

anothor = input("Do you want to perfrom another calculation? (yes or no): ")
#輸出是否將進行下一個計算

if another == yes: 
	firstnumber = float(input("Enter the first number: "))
	secondnumber = float(input("Enter the second number: "))
	arithmeoperation = input("Selevt an arithmetic operation (+, -, *, /): ")

	if arithmeoperation == +:
	result = firstnumber + secondnumber
	if arithmeoperation == -:
	result = firstnumber - secondnumber
	if arithmeoperation == *:
	result = firstnumber * secondnumber
	else:
	result = firstnumber / secondnumber	
	#計算結果

	print("Result:"+" "+result)
	#若yes則繼續上則的計算循環
else:
	print("Goodbye!")
	